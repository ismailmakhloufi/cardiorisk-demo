# Backend FastAPI

Lancer depuis la racine du projet :

```powershell
.venv\Scripts\uvicorn.exe backend.main:app --reload --port 8000
```

Endpoints :

- `GET /api/health`
- `POST /api/predict`
- `POST /api/report`
